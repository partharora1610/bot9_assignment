import React from "react"

import {
  Dialog,
  DialogContent,
  DialogDescription,
  DialogHeader,
  DialogTitle,
  DialogTrigger,
} from "@/components/ui/dialog"
import { Textarea } from "../ui/textarea"
import { Button } from "../ui/button"

interface CustomActionDialogProps {
  open: boolean
}

const CreateActionDialog: React.FC<CustomActionDialogProps> = ({ open }) => {
  return <div></div>
}

export default CreateActionDialog
